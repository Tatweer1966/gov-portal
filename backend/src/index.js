'use strict';

module.exports = {
  /**
   * An asynchronous register function that runs before the application starts.
   */
  register(/*{ strapi }*/) {},

  /**
   * An asynchronous bootstrap function that runs after the application starts.
   */
  bootstrap(/*{ strapi }*/) {},
};